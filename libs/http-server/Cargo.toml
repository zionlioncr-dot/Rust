[package]
name = "http-server"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"

axum = "0.7"

tokio = { version = "1", features = ["full"] }

metrics = { path = "../metrics" }
